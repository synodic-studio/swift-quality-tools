import ProjectDescription

/// Minimal Tuist config for the demo. Its only job is to let `tuist install` /
/// `tuist generate` resolve the SwiftSkim SwiftPM package declared in Package.swift.
let tuist = Tuist()
